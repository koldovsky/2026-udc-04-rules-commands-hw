# Dependencies
node_modules
.pnp
.pnp.js

# Builds
dist
build
out
.next
coverage

# Env / secrets — NEVER commit these
.env
.env.local
.env*.local
*.pem

# Editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Windows Git Bash safety: prevent accidental NUL device files
nul

# Local agent state (settings, worktrees) — but DO commit .claude/commands/
# (cross-tool command mirror, Task C) and .claude/rules/ (native Claude Code
# rules mirroring .cursor/rules/, Task A).
.claude/*
!.claude/commands/
!.claude/rules/
