# CLAUDE.md

See [AGENTS.md](./AGENTS.md) — it is the tool-neutral baseline for this
directory (stack, structure, commands, code style, architecture, guardrails).

Kept as a pointer rather than a copy so the two files cannot drift apart. Repo-
wide, glob-scoped rules live in `../.cursor/rules/*.mdc`; the same commands are
mirrored in `../.claude/commands/`.
