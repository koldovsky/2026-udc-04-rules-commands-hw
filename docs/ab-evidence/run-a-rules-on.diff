diff --git a/app/src/actions.ts b/app/src/actions.ts
index c491968..a0bab58 100644
--- a/app/src/actions.ts
+++ b/app/src/actions.ts
@@ -4,7 +4,7 @@
 // these helpers so the payload shape stays in one place. When you add an
 // action variant to types.ts + reducer.ts, add its creator here too.
 
-import type { Action, Filter, TaskId } from "./types.js";
+import type { Action, Filter, Priority, TaskId } from "./types.js";
 
 export function addTask(id: TaskId, title: string): Action {
   return { type: "task/added", payload: { id, title } };
@@ -14,6 +14,10 @@ export function toggleTask(id: TaskId): Action {
   return { type: "task/toggled", payload: { id } };
 }
 
+export function setTaskPriority(id: TaskId, priority: Priority): Action {
+  return { type: "task/prioritized", payload: { id, priority } };
+}
+
 export function removeTask(id: TaskId): Action {
   return { type: "task/removed", payload: { id } };
 }
diff --git a/app/src/index.ts b/app/src/index.ts
index 63e0d95..eb3fe99 100644
--- a/app/src/index.ts
+++ b/app/src/index.ts
@@ -3,8 +3,8 @@
 // write around this code, not the app itself.
 
 import { createStore } from "./store.js";
-import { addTask, toggleTask, setFilter } from "./actions.js";
-import { visibleTasks, remainingCount } from "./selectors.js";
+import { addTask, toggleTask, setFilter, setTaskPriority } from "./actions.js";
+import { visibleTasks, remainingCount, taskPriority } from "./selectors.js";
 import { slugify } from "./lib/text.js";
 
 const store = createStore();
@@ -16,12 +16,14 @@ store.subscribe((state) => {
 });
 
 const buyMilk = slugify("Buy Milk");
+const writeRules = slugify("Write rules");
 store.dispatch(addTask(buyMilk, "Buy Milk"));
-store.dispatch(addTask(slugify("Write rules"), "Write rules"));
+store.dispatch(addTask(writeRules, "Write rules"));
 store.dispatch(toggleTask(buyMilk));
+store.dispatch(setTaskPriority(writeRules, "high"));
 store.dispatch(setFilter("active"));
 
 console.log(
   "visible:",
-  visibleTasks(store.getState()).map((task) => task.title),
+  visibleTasks(store.getState()).map((task) => `${task.title} (${taskPriority(task)})`),
 );
diff --git a/app/src/reducer.test.ts b/app/src/reducer.test.ts
index 386d517..3e61151 100644
--- a/app/src/reducer.test.ts
+++ b/app/src/reducer.test.ts
@@ -1,6 +1,6 @@
 import { describe, expect, it } from "vitest";
 import { reducer } from "./reducer.js";
-import { addTask, removeTask, setFilter, toggleTask } from "./actions.js";
+import { addTask, removeTask, setFilter, setTaskPriority, toggleTask } from "./actions.js";
 import { initialState, type AppState } from "./types.js";
 
 describe("reducer", () => {
@@ -37,4 +37,23 @@ describe("reducer", () => {
     const next = reducer(state, toggleTask("missing"));
     expect(next.tasks[0]?.done).toBe(false);
   });
+
+  it("sets a task's priority", () => {
+    const withTask = reducer(initialState, addTask("a", "A"));
+    const prioritized = reducer(withTask, setTaskPriority("a", "high"));
+    expect(prioritized.tasks[0]?.priority).toBe("high");
+  });
+
+  it("does not mutate the previous state when setting priority", () => {
+    const withTask = reducer(initialState, addTask("a", "A"));
+    const prioritized = reducer(withTask, setTaskPriority("a", "low"));
+    expect(withTask.tasks[0]?.priority).toBeUndefined();
+    expect(prioritized.tasks[0]).not.toBe(withTask.tasks[0]);
+  });
+
+  it("returns the same state for an unknown id priority change", () => {
+    const state: AppState = { tasks: [{ id: "a", title: "A", done: false }], filter: "all" };
+    const next = reducer(state, setTaskPriority("missing", "high"));
+    expect(next.tasks[0]?.priority).toBeUndefined();
+  });
 });
diff --git a/app/src/reducer.ts b/app/src/reducer.ts
index 0c48fa8..ff981f7 100644
--- a/app/src/reducer.ts
+++ b/app/src/reducer.ts
@@ -26,6 +26,16 @@ export function reducer(state: AppState, action: Action): AppState {
         ),
       };
 
+    case "task/prioritized":
+      return {
+        ...state,
+        tasks: state.tasks.map((task) =>
+          task.id === action.payload.id
+            ? { ...task, priority: action.payload.priority }
+            : task,
+        ),
+      };
+
     case "task/removed":
       return {
         ...state,
diff --git a/app/src/selectors.test.ts b/app/src/selectors.test.ts
new file mode 100644
index 0000000..bc24eb4
--- /dev/null
+++ b/app/src/selectors.test.ts
@@ -0,0 +1,18 @@
+import { describe, expect, it } from "vitest";
+import { reducer } from "./reducer.js";
+import { addTask, setTaskPriority } from "./actions.js";
+import { taskPriority } from "./selectors.js";
+import { initialState } from "./types.js";
+
+describe("taskPriority", () => {
+  it("treats a newly added task as normal", () => {
+    const task = reducer(initialState, addTask("a", "A")).tasks[0];
+    expect(task && taskPriority(task)).toBe("normal");
+  });
+
+  it("reports an explicitly set priority", () => {
+    const withTask = reducer(initialState, addTask("a", "A"));
+    const task = reducer(withTask, setTaskPriority("a", "high")).tasks[0];
+    expect(task && taskPriority(task)).toBe("high");
+  });
+});
diff --git a/app/src/selectors.ts b/app/src/selectors.ts
index a6de0fb..345cf31 100644
--- a/app/src/selectors.ts
+++ b/app/src/selectors.ts
@@ -2,7 +2,11 @@
 // selectors instead of poking at `state.tasks` directly, so filtering logic
 // lives in one place.
 
-import type { AppState, Task } from "./types.js";
+import type { AppState, Priority, Task } from "./types.js";
+
+// A task with no explicit priority counts as "normal", so newly added tasks
+// behave as normal without the reducer having to stamp the field.
+const DEFAULT_PRIORITY: Priority = "normal";
 
 export function visibleTasks(state: AppState): Task[] {
   switch (state.filter) {
@@ -16,6 +20,10 @@ export function visibleTasks(state: AppState): Task[] {
   }
 }
 
+export function taskPriority(task: Task): Priority {
+  return task.priority ?? DEFAULT_PRIORITY;
+}
+
 export function remainingCount(state: AppState): number {
   return state.tasks.filter((task) => !task.done).length;
 }
diff --git a/app/src/types.ts b/app/src/types.ts
index 81caa1b..ce5db1e 100644
--- a/app/src/types.ts
+++ b/app/src/types.ts
@@ -8,10 +8,16 @@ export type TaskId = string;
 
 export type Filter = "all" | "active" | "done";
 
+export type Priority = "low" | "normal" | "high";
+
 export interface Task {
   id: TaskId;
   title: string;
   done: boolean;
+  // Optional so existing Task literals keep typechecking. An absent priority
+  // means "normal" — resolve it with `taskPriority` from selectors.ts rather
+  // than reading this field directly.
+  priority?: Priority;
 }
 
 export interface AppState {
@@ -28,6 +34,7 @@ export type Action =
   | { type: "task/added"; payload: { id: TaskId; title: string } }
   | { type: "task/toggled"; payload: { id: TaskId } }
   | { type: "task/removed"; payload: { id: TaskId } }
+  | { type: "task/prioritized"; payload: { id: TaskId; priority: Priority } }
   | { type: "filter/set"; payload: { filter: Filter } };
 
 export const initialState: AppState = {
